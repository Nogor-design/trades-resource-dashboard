# Copy these into Streamlit Community Cloud > App settings > Secrets.
# Keep your existing DEMO_USERNAME and DEMO_PASSWORD values in Cloud.

BYPASS_DEMO_LOGIN = "false"

# Use "true" only while you are logged in preparing/uploading a customer review.
# Set back to "false" before sending the customer the review link.
ENABLE_OWNER_ADMIN_TOOLS = "false"

CUSTOMER_SESSION_TTL_HOURS = "72"
PUBLIC_APP_URL = "https://trades-command-center.streamlit.app"
